extends layout

block content
  //div= mock['MA1101R']
  - var days = ['SUN', 'MON', 'TUE', 'WED', 'THU', 'FRI', 'SAT']
  table
    thead
      tr
        td.week
        td.day
        - for (var i = 0; i < 24; i++)
          if (i < 5)
            td.slot.ungodly
          else if (i > 10) && (i < 21)
            td.slot.stuff
              div= i
          else
            td.slot.empty
              div= i
            
    tbody
    - for (var daysI = 0; daysI < 7; daysI++)
      tr
        if (daysI == 0)
          td(rowspan="7").weekWrapper
            div.week Week 1
        td.day= days[daysI]
        - for (var i = 0; i < 24; i++)
          if (i < 5)
            td.slot.ungodly
          else if (i > 10) && (i < 21)
            td.slot.stuff
          else
            td.slot.empty

