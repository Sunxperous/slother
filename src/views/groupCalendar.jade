extends layout

block headScript

block content
  include calendar

  #info_controls
    .info-block
      h2#group_name= group.groupName
      h3.info-block-title Members
      ul#members
        li.hidden
          span.member-name
          span.toggle-view hide
    .info-block
      form#add_friend(action='/group/' + group.hash + '/invite')
        h3.info-block-title Invite a friend
        div
          label(for='friend_name') Username
          input#friend_name(name='url' type='text')
        div
          input#add.green(type='submit' value='Invite')

block footScripts
  script(src='/js/group.js')